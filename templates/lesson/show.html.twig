{% extends 'base.html.twig' %}

{% block title %}{{ lesson.course.name }}/{{ lesson.name }}/study_on{% endblock %}

{% block body %}
    <h1>
        <a href="{{ path('app_course_show', {'id': lesson.course.id}) }}">{{ lesson.course.name }}</a>
    </h1>
    <h2 class="lesson-name">
        {{ lesson.name }}
    </h2>

    <p class="lesson-content">{{ lesson.content }}</p>

    <a href="{{ path('app_course_show',{'id': lesson.course.id}) }}">Назад</a>

    <a href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">Редактировать</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
